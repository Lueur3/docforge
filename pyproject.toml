[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "docforge"
version = "1.0.0"
description = "Графическая обёртка над MarkItDown и Pandoc для конвертации документов"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "PyQt6",
    "markitdown[all]",
    "pypandoc",
    "pymupdf",
]

[project.optional-dependencies]
ffmpeg = ["imageio-ffmpeg"]     # audio/video support in MarkItDown
chromium = ["playwright"]        # PDF via Chromium (+ python -m playwright install chromium)

[project.scripts]
docforge = "docforge.app:main"

[tool.setuptools.packages.find]
include = ["docforge*"]   # without this setuptools would also pick up tests/

[tool.setuptools.package-data]
docforge = ["resources/*.ico"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM"]
